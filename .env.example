# Hard-coded registry URL. When set, the login form hides the URL input
# and the value cannot be overridden through the UI. Useful when deploying
# this UI as a fixed companion to a single registry.
#
# Leave blank to allow the user to enter any registry URL.
VITE_REGISTRY_URL=
